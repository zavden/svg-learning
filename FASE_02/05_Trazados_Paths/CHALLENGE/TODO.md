# TODO — Challenges sección 05

Pendiente crear los ejercicios de esta sección.

## Ideas de challenges para implementar

- **challenge_01** — Leer paths: dado un SVG con 5 paths distintos, describir en palabras qué forma crea cada uno sin renderizarlo (triángulo, flecha, línea en L, etc.)
- **challenge_02** — Convertir primitivas: reescribir un `<rect>`, un `<polygon>` y un `<polyline>` como `<path>` equivalente usando M/L/H/V/Z
- **challenge_03** — Flecha y chevron: crear una flecha apuntando a la derecha y un chevron usando solo M, L y Z; luego con bordes redondeados usando stroke-linecap/linejoin
- **challenge_04** — Onda con Bézier: crear una onda suave que atraviese el SVG de izquierda a derecha usando C + S encadenados; añadir un área de relleno debajo
- **challenge_05** — Arcos y sectores: crear un pie chart de 3 sectores (40%, 35%, 25%) usando el comando A, con colores distintos para cada sector
- **challenge_06** — Agujero con evenodd: dibujar un donut, un marco rectangular y una estrella de 5 puntas con centro hueco usando sub-trazados y fill-rule="evenodd"
- **challenge_07** — Icono complejo: crear un icono de "lupa" (círculo + línea del mango) usando un solo `<path>` con dos sub-trazados
