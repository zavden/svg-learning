# TODO — Challenges sección 01

Pendiente crear los ejercicios de esta sección.

## Ideas de challenges para implementar

- **challenge_01** — Crear un SVG válido desde cero: dado un listado de elementos y atributos, ensamblarlo correctamente con todas las reglas XML
- **challenge_02** — Detectar los errores XML: se da un SVG roto con 4–5 errores (falta de cierre, sin comillas, case incorrecto) y hay que identificarlos y corregirlos
- **challenge_03** — Decidir el formato: dado un listado de 8 casos de uso, decidir si usar SVG, PNG, JPEG o WebP y justificarlo
- **challenge_04** — Primer ícono a mano: crear un ícono sencillo (casa, flecha, check, estrella) usando solo `<circle>`, `<rect>`, `<line>` y `<polygon>` con las reglas XML correctas
- **challenge_05** — Comparación escalado: crear el mismo gráfico simple con viewBox y sin viewBox, y observar la diferencia al redimensionar
