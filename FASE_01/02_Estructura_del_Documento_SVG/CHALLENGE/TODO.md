# TODO — Challenges sección 02

Pendiente crear los ejercicios de esta sección.

## Ideas de challenges para implementar

- **challenge_01** — Completar la estructura: se da un SVG con partes faltantes (sin xmlns, sin viewBox, sin title) y hay que completarlo correctamente según el contexto (externo vs inline)
- **challenge_02** — Hacer accesible un SVG: dado un SVG sin ningún atributo de accesibilidad, añadir role, aria-labelledby, title y desc correctamente
- **challenge_03** — Usar defs correctamente: dado un SVG con un gradiente definido pero no referenciado, identificar el error y corregirlo. Luego reutilizar el mismo recurso en 3 elementos distintos
- **challenge_04** — Modelo del pintor: se da un SVG con el orden incorrecto (texto detrás de una figura, fondo encima del contenido) y hay que reordenar los elementos para que el resultado visual sea correcto
- **challenge_05** — SVG con SVG anidado: crear un panel de 3 "widgets" usando SVGs anidados, cada uno con su propio viewBox y posición (x, y) dentro del padre
